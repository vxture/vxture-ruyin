# deploy/worker-02/.env.ruyin.prod.server
# ruyin-server 生产环境容器变量
# 复制本文件为 .env.ruyin.prod.server，填写真实值。

NODE_ENV=production
RUYINAGENT_SERVER_PORT=3112

DATABASE_URL=postgresql://ruyin:CHANGE_ME@vx-ruyin-pg-prod:5432/ruyin_prod

AI_GATEWAY_URL=http://host.docker.internal:3100
