# 80-liaison 索引

与平台线来往函件。命名照 org 惯例 `NN-YYMMDDHHMM-ruyin-<slug>.md`。

待发首批（见 `../70-workplan/10-workplan.md` Liaison 表）：

- L1 桌面分发剖面报备（30-design/70 §10.1）
- L2 `dl.vxture.com` 边缘 vhost 申请（照 vxtpl edge-vhost-request 函件格式）
- L3 原生客户端对接三件（PKCE 注册 / entitlement 原生凭证 / AI Gateway 计量口径）

暂无已发函件。
