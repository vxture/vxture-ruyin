# 技术债登记（TD 寄存器，append-only）

> 依 org 偏离纪律：暂不可满足的标准条款 / 有意的过渡态，三步登记（实现处标注 + 本表记名 + 必要时报平台），
> 带回收条件；回收后状态改 closed，编号不复用。

| ID | 条目 | 原因 | 回收条件 | 状态 |
|---|---|---|---|---|
| TD-001 | Windows 代码签名证书未采购，release 签名步缺位 | 采购未启动（08 OQ-1）；未签名 = SmartScreen 拦阻 | 证书就位 → release.yml 启用签名步；stable 渠道公开发布前必须回收 <br><br>**改判 standing（2026-09-02，owner 定：不采购）。** owner 复核后认为本产品不需要这张证书；即便需要，也只接受免费方案。**已核实闭源商业产品没有免费的 Authenticode 证书** —— SignPath Foundation 的免费计划限开源项目，而本仓是明确的 all-rights-reserved 闭源（无 LICENSE、无 license 字段）；Azure Artifact Signing 最便宜但限美/加/欧盟/英国已验证企业。<br><br>**这个决定比过去更站得住**：EV 证书自 2024-03 起已被微软取消 SmartScreen 即时信誉特权，EV 与 OV 一样要靠下载量累积，且新版本 hash 变了信誉重置。也就是说**买了证书也不会立刻消除警告** —— 原条目「未签名 = SmartScreen 拦阻」暗示的因果只对了一半。<br><br>**接受的后果**：① 用户安装时会看到 SmartScreen 警告（是警告不是封锁，可「仍要运行」）；② electron-updater 在 Windows 默认校验更新包签名，未签名则装不上（见 TD-021）。状态改 standing 而非 closed：它不是被解决了，是被**有意接受**了，与 TD-005 / TD-008 同类。| standing |
| TD-002 | main ruleset 未 apply，直推 main 仍可行 | bootstrap 顺序要求先让五个 CI context 在 main 产生一次 | 已回收（2026-07-24）：fcca640 全绿后 apply，ruleset id 19652673 active，此后只走 PR | closed |
| TD-003 | CI build / test-coverage 为绿色占位（workspace 无包，recursive 空跑） | W1 治理批先行，Phase A 代码未落 | 已回收（2026-07-24）：contract-schema 23 测 + runtime-core 9 测 + local-host 2 集成测，build/test 均有真实工作量 | closed |
| TD-004 | `lint:contract`（R1–R11 契约护栏）未接入 quality-gate | 校验器与 CLI 属 W2 产物 | 已回收（2026-07-24）：contract-lint job 接入 quality-gate needs，`ruyin lint products` 全量把关 <br><br>**措辞更正（2026-09-02）**：本条写作「R1–R11 契约护栏」，**区间是错的**。校验器实际实现的是稀疏集合 **R1、R3–R5、R7–R11、R13、R14**（R2 已退役且编号不复用；R6 由 JSON Schema 的 `additionalProperties:false` 结构性挡住并回映为 R6；R12 属包层 L4，在校验器之外）。**讽刺之处**：`packages/contract-schema/src/rules.ts` 开头就写着「写成区间的地方每加一条规则就会过期一次，而过期的注释读起来和正确的一样」——本条正是那种写法。权威清单在 `docs/30-design/30-contract-schema.md` §15，不在任何区间表述里。 | closed |
| TD-005 | 契约 schema 禁入 `execute_script` 类工具（设计约束，非缺陷） | 无 OS 级执行沙箱（30-design/60 §13；同类产品 Cowork/WorkBuddy 均以 VM/容器兜底） | 执行沙箱专项立项并落地后解禁；在此之前 R 规则层面拒绝该类工具 <br><br>**机制更正（2026-09-02）**：本条称在此之前由「**R 规则层面**」拒绝该类工具 —— **R 规则里没有任何一条提到 `execute_script`**（全仓核过）。真正挡住它的是 JSON Schema 里 `category` 的**闭合枚举**（`packages/contract-schema/src/schema.ts`，只允许 local_read / local_write / query / generate / export / external_send）加上 `additionalProperties:false`。**效果成立，而且比规则更硬** —— 结构性拒绝，不依赖任何一条规则被记得去写。但描述的机制是错的：照着它去找那条 R 规则的人会一无所获，并可能以为护栏丢了。 | standing |
| TD-006 | **`products/bid` 是测试夹具，有意长期留在仓内**（不是待迁出的过渡态产品） | MVP 期同仓联调最快；契约/SDK 未稳定不宜拆 | 契约 + product-sdk 冻结后迁出为独立产品仓，走 03-A 分发通道 <br><br>**完善（2026-09-02，复核）**：原回收条件「契约 + **product-sdk** 冻结后迁出」**挂在一个尚不存在的包上** —— `packages/` 下只有 cli / contract-schema / document / runtime-core，没有 product-sdk（工作计划自己也写着它「尚不存在」）。**挂在不存在的东西上的条件永远不会被触发。**<br><br>「与 Runtime 同仓」这句也**高报了**：`products/` 有史以来只存在过 `.gitkeep` 与 `products/bid/ruyin.product.yaml`（247 行）两个文件，**没有任何产品代码**，也没有 package.json（所以虽然 `pnpm-workspace.yaml` 列了 `products/*`，它根本不是 workspace 包）。工作计划里的说法更准：**它是测试夹具**。<br><br>**真正的耦合面是三处相对路径读取**：`packages/contract-schema/src/contract.test.ts`、`packages/runtime-core/src/core.test.ts`、`scripts/dev/ui-harness.mjs` —— 迁出要先把这三处换成按包引用，否则掉的是测试覆盖。<br><br>**需 owner 定性**：若「本仓永远不会有产品代码、bid 只是夹具」成立，本条的正确形态就不是「迁出产品仓」，而是「夹具是否随契约冻结改为按包引用」—— 那会让它从 open 转 standing。 <br><br>**定性（2026-09-02，owner）：它就是个测试夹具，让页面别那么空洞 —— 本条据此转 standing。** 原来「过渡态产品，契约与 SDK 冻结后迁出独立产品仓」的框架是错的：没有产品要迁，本仓也不会有产品代码。核实吻合 —— `products/` 有史以来只存在过 `.gitkeep` 与一个 247 行的 `ruyin.product.yaml`，工作计划里早就写着「`products/bid` 是测试夹具」。原回收条件还挂在一个尚不存在的包（product-sdk）上，那种条件永远不会触发。<br><br>**作为夹具要守的三条**：① 它被三处按相对路径读取（`contract-schema` 与 `runtime-core` 的用例、`ui-harness`）—— 动它的目录结构会同时打断这三处；② 它**跟着安装包发给最终用户**（`electron-builder.yml` 的 extraResources 收 `**/ruyin.product.yaml`），所以它不只是开发期的东西；③ 因为②，它必须**看起来就是它本来的样子** —— 见下。<br><br>**②牵出的一个真问题（同日发现并已修一半）**：装机后首页会显示「标书编写」，而没有配 `RUYIN_CAPABILITY_BASE` 时跑任务走的是 `MockAIGateway`，回的是字面量 `[mock:能力名] task …, N message(s) in context` —— **它会一路走到用户面前当成工作成果**。`main.ts` 里写着「不接就说不接，『没接上』绝不能看起来像『在工作』」，**而在此之前守护进程一个字都没说过**：启动播报里没有任何一行讲用的是哪个网关。已补上该行（`capability surface: NOT configured - tasks will return mock output`）。**但日志到不了用户眼前** —— 界面要不要也说这句话，是一个未决的产品判断，见 TD-033。 | standing |
| TD-007 | 平台侧无"桌面分发型仓库 profile"标准，本仓剖面替换暂以 30-design/70 自述 | 模板家族只覆盖 Web 全栈仓（product_240 §0.3 裁定 ruyin 不适用） | liaison L1：平台确认过渡态 / 沉淀 profile 标准后，10-standards 薄索引指向之。**上一版更新（2026-09-02）判断错误，现更正**：当时读的是本机 `vxture-platform` 工作目录里检出的内容，而那个本地目录当时正停在 `docs/governance-distribution-launch-check` 这个分支上，不是 `main`——`git merge-base --is-ancestor` 直查 `origin/main` 证实**这条规则没有合入、连 PR 都没开过**，只是分支推上去了（`981d378`）。按"对 vxture-platform 只做分支+提交+push，PR/合并归平台线执行"这条既有边界，这正是**留给 owner 去开 PR、合并**的那一步，还没做。所以本条依旧是**未回应**：平台侧连"吸收一条规则"这一步都还没真正落地，更谈不上完整的 profile 标准文档。L1 函自己的 `Status` 字段先前已回填成"部分回应"，同样需要撤回，改回等待 <br><br>**已关闭（2026-09-02，owner 定）——前提被撤掉，不是被满足。** 本条记的是「等平台沉淀桌面分发型仓库 profile 标准」，而 owner 当天裁定：ruyin 是桌面分发仓，platform 那套治理标准主要约束 SaaS 平台产品，**本仓不按它走、也不等它**（同一裁定下 `packaged-smoke` 已按本仓自决提为第六个必需检查）。既然不再以它为前提，这就不是一笔欠着的债。<br><br>事实面没有变化，也不必粉饰：平台侧分支 `docs/governance-distribution-launch-check`（`981d378`）截至关闭时**仍未开 PR、未合入 main**，liaison issue `vxture-platform#131` 仍 OPEN 且除 ruyin 侧的说明外无回应。**留在 #131 跟踪即可，不再占本表一行**——一条我们已经决定不等的东西挂在 open 里，读起来像还在等。平台哪天真沉淀了标准，`10-standards` 加一行薄索引指过去就行，那是收益不是回收。 | closed |
| TD-008 | macOS / Linux（含信创）构建缺位，仅出 win32-x64 | MVP 范围裁定（08 OQ-4；信创路线见 30-design/60 §11） | 平台矩阵专项：真实国产化合同或 mac 需求触发 | standing |
| TD-009 | Workspace SQLite 库未加密（Phase A 明文） | SQLCipher 绑定与密钥托管（OS 凭据库）属独立工作量（30-design/60 §7.3） | 已回收（2026-07-24）：better-sqlite3-multiple-ciphers（SQLCipher 方案）+ 每空间独立密钥 AES-GCM 封装 + 主密钥 Windows DPAPI 保护（非 Windows 开发环境明文回退并告警，生产目标 win32 已覆盖）；files/ 区尚无内容，文件存储落地时并入同一密钥体系 | closed |
| TD-010 | 守护进程的原生模块 `better-sqlite3-multiple-ciphers` 按**宿主 Node** 的 ABI 取预编译包，但守护进程经 `utilityProcess.fork` 跑在 **Electron 内置 Node**（electron 42 → ABI 146；CI 的 Node 22 → 127）里；首次 `new Database()` 时 `require` 绑定会报 "compiled against a different Node.js version"。shell smoke 只探 /health，绑定是懒加载，故未暴露 | `scripts/release/pack.mjs` 的 `pnpm deploy` 在系统 Node 下跑 prebuild-install，未指定 `--runtime=electron --target=<electron 版本>`；electron-builder 的 npmRebuild/nodeGypRebuild 因与 pnpm workspace 冲突被关（apps/shell/electron-builder.yml） | 已回收（2026-08-30）：`scripts/native/sqlite-electron-binding.mjs` 用包自带的 prebuild-install 拉取 electron-ABI 预编译包到 `build/electron-v<abi>-<platform>-<arch>/`（与宿主 Node 绑定并存，`node --test` 不受影响）；`storage.ts` 在 `process.versions.electron` 下按运行时 ABI 选绑定，守护进程启动时做 `:memory:` 自检、失败即 FATAL 退出；shell 把守护进程异常退出传为非零码，dev smoke 与打包 `Ruyin.exe --smoke` 均覆盖；pack.mjs 在 deploy 后对 `out/daemon` 执行同一脚本；shell `start`/`smoke` 自动执行 `native:electron`。换 electron 大版本时脚本按新 ABI 重取（上游需有对应预编译，v12.11.1 覆盖到 electron-v149） | closed |
| TD-012 | **产品包签名验证未实现** —— 缺的是 **Registry 自建的根证书**，**与 TD-001 的代码签名证书是两回事** | Runtime 只信任 Vxture Registry 副署的包，而 Registry 与其根证书尚不存在（流 C 的 MVP Registry 未落，W4）。实现里**不谎称验过**：`verifySignature` 恒返回 false，生产默认（requireSignedPackages）因此拒装一切包；仅 `RUYIN_ALLOW_UNSIGNED_PACKAGES=1` 的开发模式放行，且结果标 `signed:false` | Registry 与根证书就位后，在 installer.ts 的 verifySignature 实现平台副署 + 发布者签名双验；管线其余各步不变 <br><br>**澄清（2026-09-02，owner 问及「是不是刚才那个证书问题」）：不是，两者毫无关系。**<br><br>**TD-001** 要的是 Windows **代码签名**证书（Authenticode），签的是安装器 `Ruyin-Setup.exe`，由**外部 CA 签发、要花钱买**，解决的是用户下载安装时的 SmartScreen 警告。<br><br>**本条**要的是 **Vxture Registry 自己的根证书**，签的是 `.ruyinpkg` 产品包的副署，由 **Vxture 自己签发（自建 PKI，不是采购）**，解决的是「这个包确实是 Vxture 发的」。<br><br>所以 **owner 定的「不采购证书」对本条没有影响** —— 它卡的不是钱，是 **Registry 服务本身还不存在**（流 C，W4）。**也不必急**：生产默认拒装一切包，没有风险敞口；而且 ADR-012 两级供给下，一级供给（拉契约）根本不走包、不需要信任锚。 | open |
| TD-013 | 「workspace」一词两义且同屏出现：侧栏身份区是**平台租户工作区**（令牌 `active_workspace`，C2 查询键），主区列表是**本地工作空间**（`ws_…`，一次业务作业的容器） | 本地那个词先于平台接入存在，接入后才撞上既有词表；违反通则 X-4「撞名必须改名，不得靠上下文区分」——此处连上下文都区分不了 | 改**本地**那个（平台侧是既有词表，横跨令牌/C2/三产品/console，不归本仓）。**命名已定案（2026-08-31，owner）：本地容器称「项目 / Project」。** 与契约 `workspace.type: project` 属同一定位，合并处理（不回避同名），已随 TD-015 ③ 落地。**用户面文案已全部改毕（2026-08-31）——撞名的实际伤害只发生在这里：同一屏幕上侧栏说租户工作区、主区说本地容器。**<br><br>**已回收（2026-08-31）。** owner 裁定「workspace 让给平台定义」（ADR-007），层级理清后改名范围随之明确并全部落地：用户面文案、内核符号（`ProjectRuntime` / `ProjectStore` / `ProjectMeta` …）、本地 API `/projects`、契约顶层 `project:` 与上下文来源 `project`、磁盘布局 `<dataDir>/projects/<id>/project.db`（带就地迁移并有测试）、新容器 id 前缀 `prj_`。<br><br>**两条按核查结论不改，理由记此备查：**<br>① **既有 `ws_` id 不重写。** 审计链创世哈希是 `sha256("genesis:" + id)`，**重写 id 会作废每一条既有链**；id 不透明，两种前缀并存不表达任何含义。迁移只动目录与文件名。<br>② **`Workspace Runtime` 规范名不改，且含义变准了**：从「管理工作空间的运行时」变为**「在工作区内运行的运行时」**——正是 Ruyin 实际做的事（登录取 `active_workspace`，在其中跑所订阅的产品）。<br><br>**余下**：项目按平台工作区归属与过滤尚未实现（ADR-007 的用户可见后果），另立跟踪。设计见 ADR-007 | closed |
| TD-014 | 本地 API 对齐《产品接入通则》。**D1–D3 已回收（2026-09-01）**：① X-1 错误封套 `{code, message, retryable, field?}`，23 个自造小写码全部换成带模块前缀的 SCREAMING_SNAKE；② 拒绝词表照抄 `NOT_ENTITLED` / `POLICY_DENIED` / `APPROVAL_REQUIRED`——**`QUOTA_EXCEEDED` 刻意不用**，Ruyin 不做配额门控（ADR-006），而通则自己讲过「加一个永不抛出的码，消费方会写一条永不触发的分支」；**D2 的实质是把 `product_unavailable` 一个码拆成两个**：本机停用（POLICY_DENIED）与平台未订阅（NOT_ENTITLED）混在一起，界面就永远显示错的行动入口——该引导首购时显示续费；③ X-4/B-3 撞名整改：`activate` 从「切版本」让给通则语义，切版本改 `pin-version`，生效开关 `enable/disable` → `activate/deactivate`，`enabled` 布尔 → `state` 字符串枚举（**布尔装不下 deprecated 这类中间态**）。另按 D-3 加了仓内守卫 `scripts/guardrails/check-api-shape.mjs`（接入 static-checks），三种违规均反证可抓。**D-1 的弃用并存期不适用**：本地 API 无外部消费方，守护进程与 UI 同版本同包发布，没有会被打断的消费方——这正是本条「边际成本接近零」的前提。**D7（X-2 聚合键）与 D8（workspace 撞名）此前已随能力客户端与 ADR-007 落地。** **D4/D5 已回收（2026-09-01）**：④ C2 信封保真——`SubscriptionFacts` 原样保留 status/tier/bundled/trialEndsAt/currentPeriodEnd/cancelAtPeriodEnd（**limits/quota_pools 刻意不取**：配额归 SaaS，取进来下一个人就会想用），并**把混成一个的两个公式拆开**：数据面门控 `tier!=null\|\|bundled` 决定能不能打开，界面门控 `tier!=null` 决定显不显示商业入口——混用时**被捆绑覆盖的产品会看到一个不该给他的订阅动作**；深链 intent 由 `commercialIntent()` 判定（从未订阅→subscribe，曾有已失效→renew），此前写死 `intent=subscribe` 会把续费用户引去首购页。⑤ C2 感知延迟——补两个拉取时点：**窗口重新获得焦点**（用户付完款回到应用的那一刻）与**打开产品前**；两处失败都不阻塞，沿用现有判定（ADR-003）。**D6 已回收（2026-09-01）**：⑥ 审计信封改用 X-3 字段名并补 `outcome`。**这不是改名，是重新建模**——`actor`（角色）与 `actorId`（身份）是两件事，都保留；`objectType` 由 `action` 前缀派生而非另立字段（派生不可能漂移，声明则要求每个新事件都记得填第二个字段）；`actorConsole` 恒为 null（Ruyin 不属于任何控制台，通则明说 **MUST NOT 硬编一个**）；**不填 costAmount/costUnit**（消费面字段，而 Ruyin 不计量——填了就是客户端自报计量）。<br>**链的处置已核实**：哈希按存进去时的字段名算，校验时按存储原样重新序列化，`prev_hash` 只是个哈希串——所以**新记录用新名字、旧记录原封不动，链跨两种形状仍然可验**（与 id 改名那次相反，那次是因创世哈希由 id 派生）。**绝不回写既有记录**：那会作废每一条链。消费方由 `toAuditView()` 投影到统一词表，旧记录的 `outcome` 标 `unknown`——**无从回填，也绝不猜**。<br>`outcome` 对 6 个结果不定的事件（checkpoint.decided / tool.decision / tool.executed / verification.result / task.failed / capability.retry）**必须由调用点说明，漏写抛错**；其余是「这件事发生了」的记录。`failed` 不折进 `rejected`——出错不是被拒。顺带把 `workspace.created` 改为 `project.created`（X-4 残留，该词已让给平台）。**TD-014 全部 D1–D8 回收完毕。** | 本地 API 面早于通则对照而建；通则 D-2 要求新接入方首个对外版本即满足全部 MUST | 逐条见《Ruyin 系统架构》§6（D1–D8）。D1–D8 全部落地（2026-09-01），守卫脚本 check-api-shape.mjs 防回退 | closed |
| TD-015 | ~~契约三处缺口~~ **已回收（2026-08-31）**：① `tools.input_schema` 已必填 + `x-ruyin-ref`（path / context_item）标注 + 新规则 R13（required 名字须已声明；local_read/local_write/export 类须标注 path 参数）；② limits 映射经裁定作废（配额归 SaaS）；③ `workspace.type` 与 `lifecycle` 已合并为两类业务形态 `continuous \| project`，**R2 随之退役、编号不复用**（它唯一的职责就是保证两个重复字段一致）。三份设计文档（20-runtime-contract §6、30-contract-schema §6/§11/§15、50-harness §5.2）已对齐，bid 夹具与 local-host 测试夹具同批更新。原始记录：① `tools` 缺 `input_schema`/`output_schema`（20-runtime-contract 定义了，30-contract-schema 定稿时丢失，而 50-harness §5.2 依赖它做放行前参数校验——**三份设计文档不一致**）；~~② 无处声明 `limits` 键到动作点的映射~~（**2026-08-31 裁定作废**：配额归 SaaS，Ruyin 是本地运行环境、不执行任何配额门控，没有要拦的动作点，见 ADR-006 澄清）；③ `workspace.type` 与 `lifecycle` 三取值一一对应、语义重叠 | 契约 schema 定稿早于 Tool Gate 实现与平台通则对照；R6 结构性禁 Provider 绑定是有意设计，**不在本条范围内**（理由见 ADR-004） | 三处同批做（避免连开两次破坏性口子）+ 同批补校验规则（先加字段后补规则等于开一段无人看守的窗口）。③ 已定裁：`project` 与本地容器定名**同一定位，合并不回避**，合成一个字段表达两类业务形态——**持续型**（如 CRM，一个长期项目）／**项目型**（如标书编写，每次作业新建独立容器）；原 `document`/`versioned` 归入项目型（版次是成果对象属性，非容器形态）。字段名与取值拼写待定。设计见《Ruyin 能力登记册》§5 §6。<br><br>**状态更正（2026-09-01）：三条早已全部落地，本条此前仍标 open 属记账滞后。** 核实：① `input_schema` 在 schema 里必填、`x-ruyin-ref` 与 R13 均在、用例覆盖；② schema 里没有 `limits`（裁定作废）；③ `project.type: continuous \| project` 已在 schema 与 bid 夹具中，R2 已退役且编号未复用。**「字段名与取值拼写待定」这句是当时留的，而拼写在实现时就定了** —— 一条读起来像待办、其实已完成的条目，与「读起来像做好了、其实没有」是同一个物种，只是方向相反 | closed |
| TD-016 | ~~`automated` 验证规则**无法求值**~~ **已回收（2026-08-31）**：裁定自动检查归产品侧（ADR-010），`kind` 降为代价／排序提示，automated 与 ai_assisted 走同一条能力通路；Runtime 不内建检查目录，故不存在「词表由谁定义」的问题。原始记录：契约的 `verification: [{id, kind}]` 只给名字，不说检查什么，Runtime 拿到 `source_traceability` 无从执行（50-harness §7.1 要求「确定性规则，本地执行」） | 契约定稿时把验证规则收敛为 id + kind，把「检查什么」留给了实现，而实现侧没有可依据的声明 | **已止损**（2026-08-31）：原实现硬编码返回 `passed`——那是在谎称验证通过；现改为 `pending_human` 并注明理由，规则升级给人而不是静默放行。**真正回收**需契约补「自动检查的声明方式」（候选：Runtime 内建检查目录 + 契约按名引用并带参），与 TD-015 同类，属下一批破坏性变更 | closed |
| TD-011 | **ui-workspace 与设计系统：①品牌入口已撤回（不该由 DS 发）②主包摇不散，有意接受** | 分批迁移；DS 侧 `styles/brands/ruyin.css` 待上游 | 大部回收（2026-08-30 深色工作台重设计）：全部 L1 控件与 chrome 均为 DS 件（Shell 族 + SegmentedControl/StatusBadge/Table/EmptyState/PanelCard…），--ry- 私有调色板退役，app.css 收敛为业务组装语义（T2 取值）。**余两项保持 open**：① DS 发布 `styles/brands/ruyin.css` 后由平台级 vxture.css 切换产品级品牌入口。**② 部分回收（2026-09-02）**：首页/设置/项目面板/`Workbench` 本身四个都改成 `React.lazy` + `Suspense`（登录页不再连带拉进整个工作台；侧栏要用的 `PROJECT_TABS`/`SETTINGS_SECTIONS`/`useHostChrome` 各自拆到独立小文件，否则从大组件文件里静态导入这几个常量会把整个组件连带拽回同一个同步包，异步边界名存实亡）。**但用 `rollup-plugin-visualizer` 实测后必须如实更正**：这样只把 UI 包从 1099.88 kB（gzip 298.75）降到 1058.99 kB（gzip 286.21）——首页/设置/工作台/项目面板四个视图各自只有 6.83–16.55 kB，从来不是大头。真正占地方的是 react-dom（~550 kB，压缩前，任何 React 应用都躲不掉）与 `@vxture/design-ui` 的伞包导出——它把全部约 24 个 Radix 组件（react-select/react-scroll-area/react-menu/react-slider/react-tooltip……）都编进同一张互相牵连的图里，**不看这个应用实际用了哪些**；能验证到的例子：这个应用的下拉全部走原生 `<select>`（`NativeSelect`），一次 `<Select>` 都没用过，但 `@radix-ui/react-select` 仍整份编进了产物。这张图编不散，是上游包的构建产出决定的，本仓这一侧动不了——`@vxture/design-ui`/`design-system` 都不在这个仓库里。试过再加 `manualChunks` 把 react/DS 拆成独立 vendor 分块：不但没有减少首屏字节数（manualChunks 只是把同样的字节挪到不同文件里，不删代码），还生出一条 circular chunk 警告；而且这个应用是整份跟着 installer 一起发版的（CLAUDE.md：ui-workspace 只装在安装包里），不是那种前端独立于后端频繁增量发布的网站——manualChunks 真正的价值（旧 vendor 分块跨发布版本继续吃浏览器缓存）在这种发布模型下基本用不上，遂放弃，已从 vite.config.ts 移除。**真正的回收**需要 `@vxture/design-ui` 发布可按组件摇树的子路径导出（或者证实全链路 `sideEffects:false` 后 Rollup 真能连内部互相引用一起摇掉），这是平台/DS 那一侧的活，和①同一类阻塞  **标题更正（2026-09-02）**：原题「设计系统迁移未完」已过期——迁移做完了。八个 UI 文件全部从 `@vxture/design-system` 导入，`--ry-*` 私有调色板只在注释里留了一句「已退役」，全仓仅剩两处原生控件且都写明理由（`home.tsx` 的 file input 藏在 DS 按钮之后，因为浏览器会用**浏览器的语言**画它；`pending.tsx` 的列表项容器 DS 无对应件）。留着旧标题会让读表的人一直以为还有一批组件要改，而实际剩的两件**本仓一行都动不了**。 <br><br>**两处小更新（2026-09-02，复核）**：① 体积数字已漂 —— 主包实测 **1029.58 kB**（原写 1058.99 kB），四个懒加载分块 6.85 / 7.27 / 16.43 / 16.48 kB。② 备选回收条件「全链路 `sideEffects:false`」**没点名缺口在哪**：`@vxture/design-ui` **已经声明了** `sideEffects: false`，**没声明的是 `@vxture/design-system`** —— 上游要查的就是这一环，范围比原文写的窄得多。两个子项复核后确实都未交付：DS 仍只发 `vxture.css`，design-ui 的 exports 只有 `.` / `./server` / `./styles` 三个入口，无按组件子路径。 <br><br>**①已撤回、②转为有意接受（2026-09-02，owner 定）——本条整体改 standing。**<br><br>**① 不再等 DS 发 `ruyin.css`：这件事本来就不该由上游做。** owner 指出：ruyin 全局只有一个产品，那份文件将**只服务一个消费者** —— 由共享设计系统去发布一个单消费者的品牌文件，是把定制放错了层。要改品牌就在 `app.css` 里覆盖 token，那本来就是组装层该干的事，DS 的平台级 `vxture.css` 继续作地基。**碑已改**（`main.tsx` 的 import 处），原来那句「ruyin 品牌入口在 DS 10.0.0 里还不存在」会让下一个人继续等一件不会来、也不该来的事。<br><br>**② 主包摇不散：接受，不追。** 事实不变（`@vxture/design-ui` 的 exports 只有 `.` / `./server` / `./styles` 三个入口，无按组件子路径；因此用一个组件也要把 24 个 Radix 组件打进来，主包约 1030 kB）。**接受的理由是形态**：这是桌面应用，包随安装器落在本机，不走网络下载 —— 1 MB 只影响首屏解析那一瞬，而这是 Web 交付才要紧的事。**重开条件**：若哪天这套界面要走网络交付（Local Web 访问模式对外开放、或做成 Web 版），体积就重新变成用户能感觉到的东西，那时再推上游开子路径导出。<br><br>顺带记下缩小过的范围，省得日后重查：缺 `sideEffects` 声明的是 **`@vxture/design-system`**，`design-ui` 那一层已经声明了。 | standing |
| TD-017 | ~~项目尚未按平台工作区归属与过滤~~ **已回收（2026-08-31，MVP M5 · ADR-015）**：owner 定下不变量「**不容许存在没有工作区的项目**」。`createProject` 第三参 `workspaceId` 必填（不变量由类型持有，不存在能产出无归属项目的路径）；未登录不允许新建（缺的是这个动作的主体，不是把功能藏起来）；attribution 之前的记录归为**待导入队列**并配「导入当前工作区」，**不回填**（回填是替用户猜，而归错比不归更难发现），导入**只填空白不搬家**；别的工作区的项目隐藏但**只报数量不报名字**。连带：首页「先本地使用」语义变窄，文案已同步。原始记录：项目存在 `<dataDir>/projects/<id>` 且与 `active_workspace` 无关，切换工作区看到的项目不变 | 改名前「workspace」在本仓另有含义，归属关系无处安放；ADR-007 理清层级后才成立 | 见 ADR-015 | closed |
| TD-018 | **解析归产品，不归本仓**（ruyin 侧的承载面与能力通路已交付；「读懂 PDF/Word」从来不是这个仓的活） | **承载面已通（2026-08-31，MVP M3）**：`ContextItem.content` 为 `text \| binary \| unavailable` 联合，PDF/Office/图片按字节 + 媒体类型过线，审计记 `content_kind`/`media_type`/`transmitted_bytes`。**同时修掉一个真问题**——原实现把 `[binary or unsupported file type: X]` 当内容送出，模型分不出它与真正文的区别、审计哈希的是这句我们编的话、FTS 也把这些词收进了索引。**仍缺的是解析**（把 PDF 变成文本），归 Atlas，本条继续跟踪的就是这一半。原始记录：本地文件接触只认纯文本扩展名 解析属模型能力，按 ADR-008 由 Atlas 补齐，Ruyin 不引入本地解析库。**状态更正（2026-09-02，查代码核实，推翻此前两版误判）**：这句"等 Atlas"本身站不住——ruyin 自己够到能力面的那一段（Capability Resolver，实现 ADR-009）**早就建好且测过**：`apps/local-host/src/capability-client.ts` 的 `CapabilityClient`，98.2% 行覆盖率；`host-ports.ts` 的 `MockAIGateway` 自己注释就写着"不是以后会被替换掉的东西——真正的 CapabilityClient 已经在了，配置了能力面就用它"。`main.ts` 里 `RUYIN_CAPABILITY_BASE` 一有值就切真实客户端，没配置才落到 mock。ruyin 与 Atlas 之间**架构上不存在直连关系**（ADR-001 标题即"Ruyin 不直连 Atlas"：Atlas 认调用方身份要 S2S 换票，换票要 `client_secret`，而 ruyin 是零密钥客户端，"从来就不该出现在 Atlas 的调用方名单里"），Atlas 现在是什么状态在这件事上根本不需要关心，也不该由 ruyin 去核实或找平台/Atlas 发函问 | **状态更正（2026-09-02）**：不是等 Atlas 补齐接入，是等**某个业务产品自己的云端服务**先实现 `CapabilityClient` 要调的协议（`POST /products/{id}/capabilities/{capability}/turn`，ADR-009），`RUYIN_CAPABILITY_BASE` 指过去才有得接——ruyin 这侧已经现成。`products/bid` 目前还是仓内过渡态（TD-006），没有云端对应物，这是那一侧要做的事，不是 ruyin 运行时的活。**另需一并处理一处透明度问题**：解析在云端意味着整份文件原文要外发——制度上没越界（推理传输 ≠ 数据存储，`persistence: none`），但「发一段文本做推理」与「把整份 PDF 发出去」在用户感受里不是一件事，传输门那一刻要让用户看得见外发量级。对涉密网／物理隔离客户此路不通，需等本地模型 <br><br>**两处更正（2026-09-02，复核实测）**：① 原文称 `CapabilityClient` 有「98.2% 行覆盖」——**过期，而且挑了三个数字里最好看的那个说**。实测 `96.40% 行 / 81.48% 分支 / **62.50% 函数**`（未覆盖 29-30、93-94 行）。函数覆盖 62.5% 才是这里的真实成色。此后本表不再写点位数字：它每改一次代码就过期一次，而**过期的数字读起来和准确的一样**。② 原文把「传输门那一刻要让用户看得见外发量级」整个列为待办 —— **低报了**：`workspace.tsx` 的 `context_confirm` 确认卡片**已经逐条显示名称 / 类型 / KB 体积**。真正还缺的只有**合计量**与「整份原文会外发」这句措辞，不是「看得见」这件事没做。 <br><br>**已关闭（2026-09-02，owner 定：这不是本仓要解决的问题）。** owner 的定性：**ruyin 负责把桌椅板凳摆好，菜有人做。** 解析属于业务语义，归产品 —— 与 ADR-011「Runtime 给事实，产品给措辞与判定」是同一条界线，而本条此前把它记成了自己的欠账。<br><br>**本仓这一侧全部交付完毕，逐项核过**：`ContextItem.content` 的三态联合（文本 / 字节 / 不可用）已就位；十种媒体类型按字节过线（pdf、docx、xlsx、pptx、doc、xls、png、jpg、jpeg、webp）；审计记 `content_kind`、`media_type`、`transmitted_bytes`；`CapabilityClient` 已建并接在 `RUYIN_CAPABILITY_BASE` 上。**读不懂 PDF 这件事，本仓做的是诚实地说读不懂** —— `read_file` 拒绝把非文本硬解成 UTF-8 交给模型。那正是框架该做的，再往前一步就越界了。<br><br>**不再作为本仓待办跟踪。** 某个业务产品实现解析之后，这里不需要任何改动就能接上 —— 那正是「桌椅板凳已经摆好」的意思。 | closed |
| TD-019 | 「产出本地文档」写不了二进制。**落盘接缝已通（2026-08-31，MVP M2）**：`writeArtifact(path, bytes, grants)` 带授权护栏 + 上限 + 原子改名，且**字节路径是唯一写入路径**（`write_document` 编码后也走它，护栏不会因新增写入方而漏掉）。**未加 base64 工具参数**——那是 ADR-013 否决的 A（字节流经对话），并已在会被"顺手补上"的那个位置留了说明。同批修正 `read_file` 把非文本文件按 UTF-8 硬解成乱码当正文交出（与 M3 同一毛病的另一条通路）。**C 的主体已回收（2026-09-01，ADR-016）**：新增发布包 `@vxture/ruyin-document` —— 内部表示用 **mdast**（Markdown 的标准 AST）而不自造 schema，上线格式用 **Markdown 文本**而不是手搓 AST JSON（模型写 Markdown 是母语；JSON AST 贵三到五倍且错在结构上看不出来），Markdown 说不了的分页/目录走 `remark-directive` 指令而不发明语法。渲染器输出 `.docx`（OOXML），同构，Cloud Runtime 可直接复用。**这不违反 ADR-013 对 A 的否决**：A 让模型转运一份它并没有创作的字节，C 让模型交出它本来就要创作的正文 —— 包装不过模型，正文躲不掉。`export_result` 的来源**按路径给**，不把正文贴进调用参数：那些文档上一轮已由 `write_document` 写在授权目录里，再贴一遍等于整篇二次流经对话按全价计费。**丢内容就失败**：表达不了的构件分 lossy（拒渲，回带行号的原因）与 degraded（照渲，但说出来）—— 一份悄悄少了图表的标书和一份正确的长得一模一样。用例真的解开 .docx 读 OOXML 下断言，逐个构件对，不只断言「有字节」。**顺带抓到并修掉的沉默**：`::ry:pagebreak` 用冒号写法解析器不报错，只是把它变成正文；契约声明 `pdf` 而无人实现。B 的引用取回仍未做，另立条目。原始记录：工具执行器写 UTF-8 字符串，回合协议 `content` 也是字符串 | 早期只考虑文本产出 | 按 ADR-013：以 Runtime 渲染结构化内容为主（渲染是确定性转换，属技能，归本仓），字节回传为辅；需要一种结构化文档表示、一个预制渲染技能，以及工具执行器支持写二进制 | closed |
| TD-020 | **§18.5 承诺的「本地数据可导出」尚无实现**：本地 API 没有导出端点，代码里「导出」二字只出现在**告诉用户可以导出的那句话**里。而那句话恰好显示在用户最会去找它的时刻——订阅失效、产品锁住时 | §18.5 与 40-context §9.2 都把「可访问、可导出」并列写死为数据主权底线，实现只做了「可访问」那一半；没有测试覆盖「导出」，所以缺口不显形 | **已回收（2026-09-01）**：`POST /projects/:id/export` 落地。导的是**被锁在存储里的那部分**（meta / 契约 / 业务状态 / 任务实例 / 审计链）——产出文档由 `writeArtifact` 写进用户自己的目录，本来就在他手里，再复制一份只是同一份东西存两处。**用 in-toto Statement + DSSE 信封承载，不自造清单**：审计链本来就是一份 provenance 声明，换个信封别人的工具就能读；而一个只有我们的脚本能验的证明，对收件人说服力等于零。**`signatures: []` 是合法状态不是占位**——客户端零密钥签不了，所以当前**可验篡改、不可归属**；签名身份就位后同一信封加签即可，格式不重做。predicate 字段按「能被重新表达成 C2PA 断言」设计（C2PA 词表明确覆盖「是否借助 AI 生成」，正是模型辅助产出的交付物该带的事实）。**导出走同一套授权护栏**（writeArtifact），未授权目录返回 POLICY_DENIED——导出不是特权动作。**导出本身进审计**（`project.exported`，成败都记）。用例钉死：信封形状、摘要对得上、**链能脱离 Ruyin 独立验证**（只要 SHA-256 + 项目 id + 事件表）、**改一个字节必须验不过**、未授权目录被拒。界面文案已恢复「可访问、可导出」。**一个说出口的取舍**：审计链含本机路径，而脱敏要改事件、改事件作废整条链——防篡改与可脱敏直接冲突，本版选防篡改并在信封里明写披露项。原始记录：**明确不做不依赖账号的导出**（owner 裁定 2026-09-01）：登录态是一道安全控制，去掉它等于「设备丢失即全量导出」；且注销未必是本人所为（公司删离职员工账号 / 租户被终止），那恰恰最不该开口子。正确次序是**登录态下导出 → 完成 → 再注销**。格式与范围（是否含审计链、产出文件、上下文索引）待定 | closed |
| TD-021 | **自动更新：MVP 阶段不做（2026-09-02，owner 定）** —— 检查保留，下载与安装改为用户在浏览器里自己完成 | feed 侧随打包链一起建好（W4），消费侧属壳的工作，被留到了「发布服务上线之后」；期间界面用一句占位文案顶着，而那句文案是个断言不是免责 | ① **已止损（2026-09-01）**：改为如实标注「尚未接通」，渠道开关一并收起——**没查过就说已是最新，比不提供这个按钮糟得多**；用户切到 Beta 却永远收不到 beta 包，同样只会以为坏了。② **真正回收**需接入 electron-updater，且**前置 TD-001 证书**：Windows 上 electron-updater 默认校验更新包签名，未签名就得关掉那道校验，等于把更新通道的安全性一并降了。③ 接入前需定三条策略：**有任务在跑时不装（等它停到安全点）**、自动下载还是询问用户、是否允许 stable↔beta 降级 <br><br>**已回收（2026-09-02，owner 定）——以「不做」的方式。** 前提变了：owner 定不采购签名证书（TD-001 转 standing），而 electron-updater 在 Windows 上默认校验更新包签名。于是只剩两条路 —— 关掉那道校验，等于让更新通道接受**任何**来自 feed 的包；或者不做自动安装。**选了后者**：为一个 MVP 阶段还不需要的便利去降一条安全底线，不划算。<br><br>**是拆掉，不是留着不用。** 壳里的 electron-updater 整段移除、依赖也从 `apps/shell/package.json` 摘掉；守护进程的 `POST /updates/install`、`GET /updates/intent`、`installGate`、`InstallIntentBox` 一并删除；`update-intent` 这个事件类型已无发布方，从三处词表里同时摘掉（events.ts / daemon-events.ts / api.ts）。**留着一条走不通的路，下一个人会以为它还能走。**<br><br>**保留并加强的那一半**：`GET /updates/check` 照旧，且新增 `downloadUrl` 与 `channel` —— 地址由**刚校验过的那份 feed 自己的 `path`** 拼出，落在同一个渠道目录里，**检查哪个渠道就下载哪个渠道，两者不可能不一致**；feed 里没有 `path` 就不给地址（猜出来的地址点下去是 404，用户会以为是产品坏了）。界面写明渠道 —— 不写明渠道的下载链接是有害的，用户可能正装上一个 beta 包而不自知。<br><br>**守卫方向随之反转**：`check-update-policy.mjs` 原本钉「自动更新必须按三条策略走」，现在钉**「它不许悄悄回来」**（壳里不得出现 electron-updater / autoUpdater）外加检查那一半必须继续诚实（`unreachable` 独立、地址出自 feed、渠道要显示）。反向验证过：把 `autoUpdater` 加回壳里立刻报错。<br><br>**要恢复自动更新需要什么**：一张签名证书（TD-001），或者一个明确的「接受关掉签名校验」的决定。两者都是决定，不是工作量。 | closed |
| TD-022 | **`search_knowledge` 契约里有、宿主没实现** —— `generate_proposal`（标书产品的主任务）因此在本机启动不了 | 由 TD-019 加的启动守卫照出来的：在此之前它同样跑不出结果，只是要烧满 12 个模型回合才报一句「exceeded 12 turns」，从不说原因。守卫把沉默变成了当场拒绝，问题本身一直都在 | **已回收（2026-09-01）**：`search_knowledge` 落地，标书产品四个任务现在一个不缺。检索范围**就是本任务的上下文集**——那是这次任务选出来、必要时经用户确认过的那一批；让检索伸到它之外，那道确认就成了摆设。命中但落在范围外的**报条数不报内容**：悄悄丢掉会让模型以为资料就这么薄，然后写出一份更弱的方案。回的是摘录加 id，不是整篇（整篇的位置在上下文通路）。**接检索时量出了一个更早的沉默**：FTS 用的默认 `unicode61` 分词器**对中文是零命中**——实测一段含「储能项目」的正文，查「储能」「储能项目」「电化学储能」「案例」四条全部 0 命中，因为它把一整串连续汉字当成一个 token。也就是说这个索引对中文资料一直是死的，而唯一的症状是 `FtsRanker` 的排序悄悄退化成按时间，看起来和「就是没有更相关的」一模一样。改用 `trigram` 并**迁移老库**（不回读源文件，索引行里本来就存着正文；单事务，中途崩了要么旧表要么新表，不会留下一张空的新表）。trigram 的代价说清：需要至少 3 个字符，「储能」「案例」这类两字词由 LIKE 子串扫描兜底。**没接检索能力时 `supports("search_knowledge")` 返回 false**，而不是支持了却查不到——前者让任务启动时被明确拒绝，后者让任务照跑、每次如实回「没找到」、最后交出一份查无实据的方案 | closed |
| TD-023 | **导出只有 `.docx`，没有 `.pdf`** | ADR-016 只交了一种渲染器。契约的 `format` 枚举已同步收到 `[docx]`，所以不存在「声明了没人实现」的空头承诺，但正式交付常常要 PDF | **已回收（2026-09-01，ADR-017）**：mdast → HTML（新 `renderHtml`，同构、确定性）→ 经 utilityProcess 消息通道交给壳 → 离屏 BrowserWindow + `printToPDF` → 字节回守护进程 → `writeArtifact` 落盘。**壳不自己写文件**：写入路径只有一条，壳自己写就是开第二条，而一条只贴合某个调用方的护栏，下一个调用方就绕过去了。**离屏窗 `javascript: false`**：HTML 由模型写的 Markdown 转来，逐节点构造 + 转义理论上进不来脚本，但「不需要」与「关掉了」是两回事；链接只放行 http/https/mailto/锚点。**PDF 的目录没有页码**（要页码得渲染两遍先定版），报 degraded 而不是装作正常。守护进程脱离壳跑时 `format` 里就没有 pdf，报「这套装配没有」而不是「这个格式做不了」。**打包冒烟加了一步真渲染**：整条链只有打包形态下才完整，单测到不了。**过程中抓到一个会误伤产品的缺陷**：离屏渲染窗也是 BrowserWindow，销毁时触发 `window-all-closed` → 应用退出；冒烟里第一份 PDF 排完应用就自杀了，产品里则是「用户关掉主窗口后再导出，导出完成的那一刻应用退出」 | closed |
| TD-024 | **ADR-013 的 B（引用取回）未做 —— 现架构下前提不成立**（产品跑在框架内的沙箱 iframe，不是远端产物提供方） | ADR-013 定的是「C 为主，B 为辅」。C 已落地（TD-019），B 一直没动 | 产品若有自己的排版实现（专有模板、特殊格式），能力面返回引用，Runtime 下载落盘。引用须**限定作用域并带有效期**，落盘仍走 `writeArtifact` 的同一套护栏 <br><br>**完善（2026-09-02，复核）**：原回收条件是把 ADR-013 的 B 段抄了一遍，**没有触发条件** —— 读的人无从判断该不该现在做。补上：B 的前置是**存在一个自带排版实现的业务产品**，而目前没有任何业务产品部署云端能力面，**与 TD-018 卡在同一件事上**。没有需求驱动就先建这条通路，建出来的是一条没人走、也没法验的路。<br><br>**实现处已立碑**（此前缺了三步登记里的这一步）：`packages/runtime-core/src/ports.ts` 的 `CapabilityTurn` —— 恰好三支，要加「引用」那一支就是在那里加。碑上同时点明一个易混淆处：`CapabilityResult.artifact` 是**本地写入回执**（写到哪、多少字节），**不是**可取回的引用，别把它当成 B 的雏形。 <br><br>**已关闭（2026-09-02，owner 定）—— 不是「暂时不做」，是这套架构不问这个问题。**<br><br>owner 的定性：**具体产品就是运行在 ruyin 框架里的应用**（类比 macOS 与它上面的 App），产品的构建产物进的是**本框架的页面内**。架构里本来就这么写着：`60-technical-architecture.md` 的 T6「产品 UI 装载 = **沙箱 iframe + postMessage 桥**」，§4 进程图里也是「产品 UI 沙箱（iframe × N）」。<br><br>而 ADR-013 的 B 设想的是另一种形态：产品是个**远端产物提供方**，回一个带作用域与有效期的引用，Runtime 去下载落盘。**产品就在本机框架内跑的时候，没有远端产物可取** —— 它自己渲染在自己的界面里，要落盘就走已有的那条唯一字节通路（`writeArtifact`，护栏、上限、原子改名都在那儿）。为一个不存在的形态先建通路，建出来的是没人走、也没法验的路。<br><br>**ADR-013 不改**：决策记录 append-only，B 作为设计选项仍然在案。**重开条件**：出现一个**没有框架内界面、纯远端**的产物提供方时，B 的前提才重新成立。 | closed |
| TD-025 | **本机跑不了打包冒烟（②已回收）；剩下的 ① 就是 TD-001，不是独立一笔** | **原判据是错的，已实测推翻（2026-09-02）。** 原文写的是「应用控制按文件哈希放行，新构建的未签名 exe 一律拒绝执行」——复核时在本机实跑：Smart App Control 仍是**开启强制**（`VerifiedAndReputablePolicyState=1`，WDAC 用户态强制状态 `2`），打出来的 `Ruyin.exe` 确认 `NotSigned`，而它**照常启动并跑完了冒烟**（`[shell-smoke] OK`）。差别在 Mark-of-the-Web：SAC 拦的是**带下载来源标记**的文件，本地构建产物没有这个标记，所以不受拦。原文把「未签名」当成了拦截条件，实际条件是「来自下载」。<br><br>**本机现在确实跑不了打包，但换了个原因**：为了让应用图标真的写进 exe，`signAndEditExecutable` 必须打开（图标是在「编辑可执行文件」那一步写入的），而打开后 electron-builder 要解压 winCodeSign 工具链，那个 7z 里带 darwin 的 dylib 符号链接，解压需要 `SeCreateSymbolicLinkPrivilege` —— 本机没开开发者模式也没提权，卡在这里。CI runner 有这个权限（已验证）。开发模式（`electron .`）两种情况都不受影响 | **② 已回收（2026-09-02，owner 定）**：CI 加了 `packaged-smoke` 作业（windows-latest，跑 `pack.mjs --dir`，含真渲染一份 PDF 的自检），**提为第六个必需检查，不再等平台标准合入**——owner 裁定：本仓是桌面分发仓，platform 那份治理标准主要约束的是 SaaS 平台产品（部署态服务、tag→环境），ruyin 从骨架起就不适用模板家族这一条（product_240 §0.3），没有理由让一条本仓自己已经验证过价值的 CI 规则去等一个主要为别的仓形态设计的标准走完流程。平台侧那条同源规则（分支 `docs/governance-distribution-launch-check`，`981d378`）截至此次回收仍未开 PR、未合入 `vxture-platform` 的 main（已发 liaison issue `vxture-platform#131` 请platform 线自行处理，不阻塞本仓）——本条回收与它是否落地无关。已在 `docs/50-deployment/rebuild/main-ruleset.json` / `CLAUDE.md` / `ci.yml` 三处落定。线上 ruleset（id `19652673`）也已 apply（2026-09-02），六项必需检查**真的在拦合并**了 —— 改仓库文件不会改 GitHub，这是分开的第二次动作，已经做完。① **TD-001 的代码签名**仍未做。注意它和本机能不能跑打包**没有关系**（那是原文的误判，见左栏）——签名要解决的是**最终用户**那一侧：用户拿到的安装包是下载来的，带 Mark-of-the-Web，SAC 会拦，签名后走发行者信誉才放行。本机的构建产物没有这个标记，从来不受这道门影响。<br><br>本机现在跑不了打包的原因是 `SeCreateSymbolicLinkPrivilege`（左栏），**解法是开开发者模式或提权，不是等签名**。打包形态仍然由 CI 验 —— 那里两件事都成立。**②已回收，①仍 open，本条整体状态不变** <br><br>**合并说明（2026-09-02）**：本条 open 的唯一理由是 ① 指回 **TD-001 的证书采购**——同一笔账在登记表里出现了两次，读表时「等证书」会被数成两条。②（CI `packaged-smoke` 提为第六个必需检查、线上 ruleset 已 apply）已经回收完毕，本条**自身不再有独立工程量**。证书到位那天，TD-001 与本条一并关闭。本机现在打不了包是另一件事，解法是**开开发者模式或提权**，与证书无关（见左栏）。 <br><br>**已关闭（2026-09-02）**：② 早已回收；① 指向的 TD-001 已由 owner 定为不采购、转 standing。本条至此**没有独立存在的理由** —— 它一直只是 TD-001 的影子，留着只会让「等证书」在表里被数成两条。本机打不了包是另一回事，解法是开开发者模式或提权（左栏已记），与证书无关。| closed |
| TD-026 | **`sources: [project]` 的上下文此前无人兑现**——任务产出不流向下游 | 契约把「需求矩阵」「技术方案」「覆盖报告」这类**由本项目任务产出**的资料声明为 `sources: [project]`，但运行时的上下文选取只认用户绑定的目录：没有绑定 → 候选为空 → 该类型被跳过。这些类型多半 `required: false`，所以既有的必需检查一个都不会响。实测过的样子：`coverage_verification` 的上下文是 `[]` —— 一个专门做「逐条对照需求矩阵与技术方案」的任务，两份文档一份都没拿到，照跑不误，而它的约束里写着「不得抽样」。`output_types` 在此之前只被契约校验用到，运行时一次都没用过 | **已回收（2026-09-01）**：任务写出的产物登记进项目成果表（类型取自**产出它的那个任务的 `output_types`**——契约已经说了它产出什么类别，不必让模型再说一遍；声明了不止一种时**不猜**，留一条 `artifact.untyped` 审计，因为标错类别的资料下游会正常地用它）。选取阶段据此兑现 `project` 源，读取仍走本地文件（它就落在授权目录里），`source` 记成 `project` 以便审计说得清出处；授权撤销后不再出现。同时：**声明了输入却一份资料都没拿到的任务不许开跑**（逐类型看都不必需，合起来「什么都没有」是另一回事），以及同一文件在两个类型下命中时按 `input_types` 先后去重——同样的字节送两遍，成本翻倍而信息没多 | closed |
| TD-027 | **本地 API 承诺的 WebSocket 事件流一直没有**，界面与壳各自轮询 | `server.ts` 的文件头从第一天起就写着「WebSocket event stream arrives in a later batch」，这句话到今天仍然是真的 —— 它是本次头部审计里唯一**没有过期**的那一条，但它从未进过技术债登记，所以也从来没有人排过它。当前代价是五处轮询：项目面板 1–5 秒、待办 / 用户 / 工作台各 5 秒，加上壳里的待确认与更新意图两个轮询。任务跑起来时 1 秒一轮，静止时 5 秒 —— 静止时是纯浪费，跑动时又慢得能被看出来 | **已回收（2026-09-01）**：`GET /events` 走 **SSE** 而不是 WebSocket —— 这些事件全是单向的，WS 要多一套连接管理，换不来任何东西。事件**只说什么变了，不带业务数据**：带了就等于开出第二条数据通路，而那条路上的护栏（授权、工作区边界、审计）要重新写一遍；收到事件的人回头照常查一次，走的还是原来那条路。客户端用 fetch + ReadableStream 而不是 EventSource，因为 EventSource 设不了请求头，会话令牌就只能塞进 URL。**轮询没有删掉，降成 30 秒兜底**：流断掉的样子是「一直没有事件」，而那和「一切正常」长得一模一样。服务端每 25 秒发一次心跳注释，断连时摘掉监听者（用例钉了这一条——否则每开一次页面漏一个）。项目面板从 1–5 秒、待办从 5 秒都改成事件驱动。**壳里那两处轮询还没改**（待确认通知与更新意图），另记 | closed |
| TD-029 | **壳仍在轮询守护进程**：待确认通知与更新意图两处，各 5 秒 | TD-027 把界面那几处换成了事件流，壳没跟上。壳是 Node 侧，fetch 流式读同样可用，只是要处理守护进程重启后的重连 —— 界面在页面刷新时天然重连，壳不会 | **已回收（2026-09-01）**：壳复用同一条 `/events`。`pending`/`task` 驱动系统通知，新增 `update-intent` 事件驱动安装意图取件（服务端在记下意图时发）。**重连显式写了**：指数退避到 10 秒上限——守护进程重启期间不该每毫秒敲一次门，但再久用户会先注意到通知停了。**两处轮询一条都没删，只降到 60 秒兜底**：断流的样子是「一直没有事件」，而那和「一切正常」长得一模一样。`RuntimeEvent` 的词表此刻抄了三份（守护进程发、界面收、壳收），由 `lint:shared-shapes` 比对——漏掉一种事件不会报错，只会让那一类变化再也不被察觉 <br><br>**后续（2026-09-02）**：`update-intent` 这个事件**已经不存在了** —— 自动更新整段拆掉（TD-021），它随之没了发布方，从三份词表里一并摘除。上面那段是当时的实况，不改写；留这一句是因为**一条已关闭的记录若点名了一个现在找不到的东西，读的人会以为是自己看漏了**。事件流本身没变，仍然是 `pending` / `task` 两种驱动壳的系统通知；轮询兜底也还在（现在只剩待确认这一处，60 秒）。 | closed |
| TD-028 | **守护进程做得到、界面够不着的能力** | 端点在，用户点不到，等于没有。`POST /products/install`（装 .ruyinpkg）、`POST /products/:id/pin-version`（§18.4 回滚）、`POST /products/:id/activate\|deactivate`（§18.5 本机生效开关）此前均无界面入口。**已补上两项**：`POST /projects/:id/export`（§18.5 那句「本地数据仍可访问、可导出」就印在同一个页面上，说了做不到比不说更糟）与 `activate\|deactivate`（产品列表把 `state` 显示出来了，却没有任何地方能改它 —— 现在已装卡上有「停用」，被挡的卡上有「启用」，且只对本机停用的产品给，未订阅的不给，因为那不是这台机器能解决的事）。**再补一项**：版本回滚（§18.4）—— 已装卡上只在真有第二个版本时出现版本下拉，一个只有一个选项的下拉框不是选择，是噪音。**余下一项**：包安装 | **全部补齐（2026-09-01）**。包安装用 **file input 而不是壳里的原生对话框**：同一个页面在浏览器和壳里都要能用（Local Web 访问模式从第一天起就成立），壳里 file input 一样弹系统选择框，少一条只有壳能走的路。字节直接 POST，结果照实说签没签名——未签名的包是另一回事，不该和签过的长一个样。用例钉了返回形状恰好是界面声明的三个字段，以及坏包返回 X-1 的 PACKAGE_REJECTED（这是安装未知来源内容的入口）。原始的回收条件记录：**这条记录此前写着「pin-version 要先让 ProductInfo 带上版本列表」，那是错的**：服务端的 `ProductView` 一直带着 `versions`，是界面那份副本漏了它 —— 一次形状漂移把一个已经能做的功能写成了做不了，现在由 `lint:shared-shapes` 挡住 | closed |
| TD-030 | **`test-coverage` 检查名不副实：从 TD-003 关闭那天起就只跑测试，从未量过覆盖率** | TD-003（2026-07-24 已回收）把这个必需检查从「无包空跑」变成「跑真实的用例集」，但没有人在那之后加过 `--experimental-test-coverage` 或任何阈值——`test-coverage` 这个名字这些月来实际做的事和一个叫 `test` 的检查完全一样。实测各包真实覆盖率后发现的具体窟窿：`platform.ts`（OIDC 登录/会话类）17% 函数覆盖，`server.ts`（HTTP 面）63% 函数覆盖，`pdf.ts`（壳渲染通道）0%——一个从未被任何用例加载过的文件，`loadProducts`（开发态产品扫描）也是 0%，`packages/cli` 连 `test` 脚本都没有 | **已回收（2026-09-02）**：五个有测试的包（runtime-core / local-host / document / contract-schema / cli）各自的 `test` 脚本加了 `--experimental-test-coverage` + `--test-coverage-include="dist/**/*.js"`（只算自己的产物，一个依赖包覆盖率的变化不会把无关包的检查翻红）+ 四项各自的行/分支/函数阈值（均留了安全余量，不是卡在当下这个数字上）。同批把上面点名的窟窿都补了实测：platform.js 17%→88% 函数、server.js 63%→86% 函数、pdf.js 0%→100%、products.js 0%→100%、packages/cli 0%→100%（新增 main.test.ts + test 脚本）。`apps/shell` 与 `apps/ui-workspace` 仍无 `test` 脚本，`--if-present` 静默跳过——不在这次回收范围内，见 TD-031 | closed |
| TD-031 | **`apps/shell`（Electron 壳）与 `apps/ui-workspace`（React 界面）没有测试** | 两者的 `package.json` 都没有 `test` 脚本，`pnpm --recursive --if-present test` 因此静默跳过它们——`test-coverage` 这个必需检查从未验证过这两个应用的任何一行代码。壳是 Electron 主进程（`main.ts` 557 行、`pdf.ts` 87 行），测试需要打桩 Electron API（`app`/`BrowserWindow`/`utilityProcess`/`ipcMain` 等）；`ui-workspace` 是 React，测组件需要引入一套本仓此前没有的工具链（jsdom + 一个组件测试库），是比其余各包单靠 `node:test` 更重的一步 | **已回收（2026-09-02）**：两半各自按原计划独立落地。壳（#82）把可测的真实逻辑（daemon 事件解析、PDF 自检协议、待办去重通知）抽成纯函数模块，`node:test` + 手工打桩（同 `apps/local-host/src/pdf.test.ts` 的 `parentPort` 先例），21 测，100%/96.67%/100%（行/分支/函数）；Electron API 本身（`app`/`BrowserWindow`/`ipcMain`）留作薄胶水层不直接测，抽取到此为止是有意的范围，不是漏项。`ui-workspace`（#83、#84）先引入 Vitest + jsdom + Testing Library 工具链，再逐一测完六个此前 0% 的大组件——login.tsx、user.tsx、settings.tsx、home.tsx、workbench.tsx、workspace.tsx——166 测，全量基线 88%/92.3%/86.66%/90.25%（语句/分支/函数/行），六个组件各自 90–100%。过程中写用例挖到并修了三个真 bug（settings.tsx 的 install 失败误标成"检查失败"；workspace.tsx 首次加载失败卡死在"加载中……"、以及一次点击悄悄双发全部五个项目端点）。两个应用的 `test` 脚本都已接入 `test-coverage` 必需检查，不再被 `--if-present` 静默跳过 | closed |
| TD-032 | **装机态与开发态共用同一个 Chromium 配置目录,登录会话互相串** | `main.ts` 里 `app.setName("Ruyin")` 是**无条件执行**的,于是两种形态下 `app.getPath("userData")` 都指向 `%APPDATA%Ruyin`。业务数据是分开的——开发态走 `~/.ruyin/dev`,装机态走 `userData/data`——但 **Chromium 自己那一份不分**:cookie(`Network/`)、`Local Storage`、`Session Storage`、`Preferences` 全在同一个目录里。<br><br>2026-09-02 实测确认,不是推断:清空后只跑开发态,它自报 `data dir: C:Users....ruyindev`,**同时把 `%APPDATA%RuyinNetwork` 和 `Session Storage` 的时间戳刷新了**,而装机态的 `userData/data` 原封未动。<br><br>后果不只是「省得重新登录」:① 开发态可能指向**另一套平台环境**,那份 token 和正式环境的躺在同一个 cookie jar 里;② 项目归属按会话里的工作区判定(ADR-015),两种形态互相继承会话意味着**项目可能被记到另一个工作区名下**;③ 清理时看不出来——用户以为卸载了装机版就干净了,开发态的登录态还在原地 | 判断这是不是有意的。若不是:开发态用 `app.setPath("userData", ...)` 指到独立目录(例如 `~/.ruyin/dev/chromium` 或 `%APPDATA%Ruyin-dev`),让整个配置分家,而不是只分业务数据那一层。改完要验的是同一件事:跑开发态,确认 `%APPDATA%Ruyin` 的时间戳不再被碰 <br><br>**已回收（2026-09-02，owner 定：不是有意的）**：`main.ts` 在 `setName` 之后加了一句 —— 非打包态把 `userData` 指到 `~/.ruyin/dev/chromium`，让**整个 Chromium 配置**跟着业务数据一起分家，而不是只分后者。放在同一个根下，删掉 `~/.ruyin` 就等于把开发态清干净，不必再记住「还有一份在 AppData 里」。必须在 app ready 之前调用，会话一旦建起来路径就定了。<br><br>**按原定判据验收，逐条对上**：清掉 `~/.ruyin` 后只跑开发态，`%APPDATA%Ruyin` 的 `Network` / `Session Storage` / `Preferences` 三个时间戳**一个都没动**（仍停在修复前那一刻），开发态的 Chromium 配置出现在 `~/.ruyin/dev/chromium/`，守护进程照常起在 7420、data dir 仍是 `~/.ruyin/dev`。<br><br>副作用如预期：开发者本机会掉一次登录态（旧会话还在 `%APPDATA%Ruyin` 里，新路径是空的）。 | closed |
| TD-033 | **没接能力面时，界面不告诉用户「这个产品还不能真干活」** | 装机包里带着 `products/bid` 这个夹具（TD-006），首页因此显示「标书编写」。没有配 `RUYIN_CAPABILITY_BASE` 时，任务走 `MockAIGateway`，回的是字面量 `[mock:能力名] task …, N message(s) in context` —— **它会一路走到用户面前当成工作成果**。守护进程启动时已如实播报（2026-09-02 补上，此前一行都没说，而 `main.ts` 的注释却写着「不接就说不接」），**但日志到不了用户眼前**。这与「数据加密」那处的先例是同一类：明文主密钥在设置页有一个「开发态」徽章明说，而「能力面没接」没有对应的说法 | **需 owner 判断，不是纯工程问题**：① 在产品卡上标一个「未接通」状态（像 `不可用的产品` 那样），代价是 MVP 演示时首页就摆着一个显眼的负面标记；② 只在任务真的返回 mock 输出时说，代价是用户已经等完了一轮才知道；③ 判定这在 MVP 阶段可接受（内部演示为主、真实产品接上后自然消失），不做 —— 那就把本条转 standing。**注意判据不是「会不会误导演示观众」，而是「一个拿到安装包的人能不能分清『在工作』与『没接上』」** <br><br>**已回收（2026-09-03，owner 定 ①）**：产品卡标「未接通」。守护进程 `/system` 多报一个字段 `capabilitySurface: configured / mock`（判定在 daemon，界面不重算）；首页每张**能打开的**卡在 daemon 明确说 `mock` 时并列显示「未接通」徽章与一行说明「现在发起任务只会得到占位输出，不是真实成果」。三条边界：① 与「已订阅」并列而不是替换 —— 两件事都成立；② **缺失 ≠ mock** —— `/system` 没回来是「不知道」，不标；③ 卡仍可打开 —— 判据是分清，不是拦住。打不开的卡（未订阅/已停用）不标：它不会跑任务，也就没有「拿占位输出当成果」这回事。代价照单全收：没接真实产品前，演示首页就摆着这个标记 —— 那正是它的用途。 | closed |
| TD-034 | **连接器的工具面还没接到 Tool Gate 上** | MCP 连接器天然提供 `tools/*`（回写内网系统、触发内部流程），ADR-005 要求它们经 Tool Gate 且写操作按 `external_send` 硬底线 ≥ ask。B 批（2026-09-03）只接了读（resources）。卡住的不是代码量，是**映射从哪来**：契约 `tools[]` 声明的是产品要什么，连接器暴露的是它有什么，两者之间那张表（谁声明、谁核准、以什么 category / risk 落到闸门）没有设计依据，猜一个就是替 owner 定 | **owner 定映射来源**（契约声明 vs 安装时声明 vs 两者都要），然后：`mcp-client.ts` 加 `tools/list` + `tools/call`，`ToolExecutorPort` 出一个按连接器路由的实现，闸门按映射表决策。工作计划「通路二 D」 <br><br>**已回收（2026-09-03，owner 定：契约声明）**：契约 `tools[]` 加可选 `provider: runtime / connector`，映射就是 id 相同 —— 契约说要什么，连接器暴露同名工具即接通；category / risk / default 仍由契约定，`decideTool` 一字未改。新增 R15：连接器工具只能是 `query` 或 `external_send`（写进内网系统出边界，硬底线 ≥ ask）。内核把 `provider` 与**本项目授权的连接器 id** 一并交给执行器；执行器只路由到授权且暴露该工具的连接器，装了没授权的看不见，两个都暴露不猜、报歧义；机器上没有连接器暴露时 `startTask` 前就拒并点名。`tool.executed` 审计记 `connector`。MCP 客户端补 `tools/list` / `tools/call`，非文本内容点名省略。**没做的**：连接器运行中新增工具不刷新（启动时 `tools/list` 一次；契约本来就要预知工具名）。 | closed |
| TD-035 | **MCP 只有 stdio 传输，没有 Streamable HTTP** | 设计（60-technical T8）写的是两种标准传输都要。B 批先做 stdio：局域网 / 私有服务多以本机进程形态提供，且 stdio 的协议面小到能用假服务器完整测试；HTTP 传输要会话、SSE 流、鉴权头，面宽一截。**自实现 vs 官方 SDK 的取舍到时重评**：官方 SDK 带 express / hono / jose 进随安装包发出的守护进程，这是当初不用它的理由；若 HTTP 传输把自实现推到与 SDK 相当的体量，就换 SDK —— 只有 `mcp-client.ts` 知道 JSON-RPC，替换是一个文件 | 出现第一个只以 HTTP 形态提供的目标连接器；或 D（TD-034）落地时一并评估。工作计划「通路二 E」 | open |
| TD-036 | **连接器安装不验签名，生产默认拒装** | ADR-005 信任模型第一、三条（来源受限、签名验证）复用 `.ruyinpkg` 管线，而那条管线的信任锚（Registry 根证书）还不存在（TD-012）。一个装好的连接器是以用户权限跑的任意进程 —— 正是「不接受任意来源」要挡的东西。B 批照包的先例：生产 `POST /connectors` 一律 403 并点名本条与 TD-012；`RUYIN_ALLOW_UNSIGNED_CONNECTORS=1` 仅开发放行，且启动播报明说「unsigned installs ALLOWED」。**不是风险敞口**：默认拒绝，不存在一条不经开关就能装东西的路 | TD-012 的 Registry 根证书就位后，连接器包走 `.ruyinpkg` 安装管线（验副署 + 发布者签名），`/connectors` 的直装口随之退役或只留开发态 | open |
| TD-037 | **静态产品库未签名：index.json 只靠 TLS，包在正式版里能看不能装** | 流 C MVP 形态（70-repo-organization §7.4）落地时（2026-09-03）没有 Registry 根证书（TD-012）：`ruyin registry` 产出的 index.json 无签名，包无 SIGNATURE 条目，清单逐条写 `signed: false`。守护进程读 index 只信到 dl 主机的 TLS 为止；下载与清单核对 size / sha256 / 同源，挡的是「拿到的不是清单说的」，挡不了「清单本身被换」。安装走既有管线，生产默认拒装未签名包 —— 所以正式版首页的「从产品库安装」只能列出、不能装，界面写明「本机不装未签名包」而不是藏掉按钮。**不是风险敞口**：默认拒绝；开发开关放行时启动播报明说 | TD-012 的根证书就位后：① `ruyin registry` 对 index.json 出签名（平台副署），守护进程验签后才信清单；② 包走 §18.2 双签，`verifySignature` 落实，生产可装。两步都做完本条才关 | open |
| TD-038 | **分发面是过渡态：GitHub Releases 代替尚不存在的 dl 主机** | 设计（70-repo-organization §7）的下载主通道是 `dl.vxture.com`（liaison L2），至今未落地。2026-09-03 之前 release.yml 的 publish 是占位：产物只暂存在 run 上（90 天过期），守护进程的更新 feed 指向不存在的主机，「检查更新」永远查不到。现改为发到 GitHub Releases：版本化 release + 按渠道滚动的 release（tag `stable` / `beta`），更新 feed 缺省指向滚动 stable，manifest.json 的 url 指向滚动 release。**不是最终形态**：地址在 github.com 上（境内可达性看网络），静态产品库目录树进不了平铺的 release 资产 | L2 落地后：publish 加 rsync 到 dl 主机（与 GitHub Releases 可并存一段时间），`DEFAULT_FEED_BASE` 与 make-manifest 的基址切回 dl 渠道目录，网站下载页改指 dl；届时本条关 | open |
| TD-039 | **数据目录改动：已实现「重启期迁移」，仍差一个系统目录选择框** | owner 2026-09-04 先问「是否让用户可修改」，后定「1、2 做」：安装期可选目录 + 改目录后重启迁移。原来的顾虑（运行中搬移半途失败会留下两份加密数据）由**时点**解决 —— 迁移发生在守护进程启动、打开任何库之前（`apps/local-host/src/data-location.ts` 的 `applyPendingMove`，在 `KeyManager.open` 之前那一行），那一刻没有任何句柄。同卷逐项改名并在失败时原路搬回；跨卷复制 → 逐文件核对大小与摘要 → 写 `done` 标记 → 才删源；`done` 与 `copying` 两种断电现场分别收尾与丢弃。指针文件 `location.json` 放在 userData 根（不跟着数据搬走）。安装期那一页由 `apps/shell/installer/data-dir.nsh` 提供，只写指针、不碰数据，已有指针时整页不出现 | 缺的是**界面里的系统目录选择框**：窗口是纯 Web 客户端（无 preload，契约边界是 HTTP），弹系统对话框只有壳做得到，要再走一次事件总线中转（与 `ui-theme`、`app-restart` 同一个机制）。当前是输入 / 粘贴绝对路径 + 校验。另外 NSIS 那一页 CI 覆盖不到（packaged-smoke 跑的是解包后的应用，不跑安装器交互），只有 makensis 的语法错误会被构建挡下 —— 有真机安装验证之后再收 另外：默认位置 2026-09-05 从漫游 `%APPDATA%Ruyindata` 改到本地 `%LOCALAPPDATA%Ruyindata`（漫游目录在域环境里会随登录/注销整份同步，而项目库是 GB 级的）。**已有机器一个字节都不搬** —— `resolveDataDir` 探到老位置有数据就写一条指针把它钉在原处 2026-09-05 补两处（实测发现）：① 搬家发生在守护进程开库之前，而壳等 `/health` 原来是**固定 15 秒**——实测 136 MB / 4001 文件的跨卷复制 + 逐文件核对 11.7 秒，也就是超过约 180 MB 必然超时，超时路径是 `app.exit(1)`（连窗口都不开），且下一次启动会丢弃半成品重来 → **数据越多越是再也起不来**。现在按待搬字节数算等待上限（`shell/migration-wait.ts`，字节数在排队时记进指针），并在搬家期间显示一屏「正在搬移数据」；② `performMove` 在源目录不存在时会返回 `moved` 并把指针改指空目录（数据在没插的移动硬盘上时正好命中）——改为报失败且指针不动。仍差**真实进度**：那一屏现在是不确定进度条。要做进度就得让守护进程在搬家期间先服务（listen 先于开库），把已搬字节数报出来；界面里的系统目录选择框**已做**（2026-09-05：folder-pick.ts 中转 + 壳弹 showOpenDialog，请求挂着等结果）；换目录也从常驻表单改成「一个按钮 + 弹层」（owner：一次性操作不该常驻页面） | open |
| TD-040 | **「宽松」密度在上游尺度里与「默认」等值，本仓自行撑开** | 实测 `@vxture/design-tokens@3.0.0` 的 `.density-comfortable`：`--space-none/2xs/xs/sm/md/lg` 与 `:root, .density-default` **逐字相同**，`--space-row-*` 也一样，只有 `--space-control-*` 抬了一档。于是宽松档下只有控件本身变高（分段控件 32 → 36px），卡片内边距 16/16、卡片间距 10/10、板块间距 26/26 全不动 —— owner 2026-09-04 报「宽松没啥太大变化，基本等于默认」。紧凑档正常（它整套折半）。本仓在 `apps/ui-workspace/src/app.css` 用 `html.density-comfortable` 逐条抬自己的容器（卡片、状态卡、产品卡、设置行与板块、设置页那一摞），并把写死的 `--set-row-h: 44px` 改成按密度取值（36 / 44 / 52）—— 后者是本仓自己的缺陷，连紧凑档都推不动设置页的行 | **不改写 `--space-*` / `--spacing-*` 的尺度**是有意的：那等于在产品仓里分叉组织级尺度。上游（design-tokens）补齐宽松档的内缩与 row 尺度后，把 app.css 里那一段 `html.density-comfortable` 名单整体删除，`--set-row-h` 改为取 DS 的 row 尺度 | open |
| TD-041 | **项目的「文件区」只在设计里存在：实现不收原件，也没有文件级加密** | 设计（30-design/60 §7.1）给每个空间画了 `files/`（内容寻址、哈希命名），§7.3 写它「同密钥体系加密」。实现里这个目录此前**只被 mkdir 出来、没有任何写入方**（owner 2026-09-05 查证），已删掉那行 mkdir —— 空目录会让人以为附件原件在里面。当前的做法是：参考资料由工具按目录授权从**用户自己的位置**读，抽出的内容写进加密库（project.db，SQLCipher），产出物写到用户授权的导出目录；数据目录里因此没有明文业务文件 | 要收原件就得连**文件级加密**一起做（拷进 `files/` 而不加密会让「数据目录整体加密」变成假话）：内容寻址命名 + 每文件用工作区密钥封装 + GB 级文件分块。触发条件：出现「投标文件与其依据必须可复现」这类要求 —— 那时「原件被用户挪走/删掉后追溯断掉」才是硬伤 | open |
